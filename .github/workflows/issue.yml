name: isseu example
on:
  issues:
     types: opened

jobs:
  issue-job:
    runs-on: ubuntu-latest
    steps:
      - name :  logs de body en title van de nieuwe issue
        run: |
          echo "issue title : ${{github.event.issue.title}}"
          echo "issue body : ${{github.event.issue.body}}"
